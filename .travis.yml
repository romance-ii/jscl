language: lisp
sudo: required
dist: trusty

env:
  matrix:
    - LISP=sbcl

install:
  - curl -L https://github.com/luismbo/cl-travis/raw/master/install.sh | sh

script:
  - make
  - make test

addons:
  srcclr: true
